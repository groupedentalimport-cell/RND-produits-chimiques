# ChemStab Industrial v5.2 — Python Dependencies

# ── Core ───────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── Database ───────────────────────────────────────────────────────────
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.0

# ── Task queue ─────────────────────────────────────────────────────────
celery==5.4.0
redis==5.1.0

# ── ML / Science ──────────────────────────────────────────────────────
numpy==1.26.4
scipy==1.14.0
scikit-learn==1.5.0
pandas==2.2.0
joblib==1.4.0

# ── RDKit (molecular descriptors, SMARTS, fingerprints) ──────────────
rdkit-pypi==2024.3.5

# ── Thermodynamics ────────────────────────────────────────────────────
CoolProp==6.6.0

# ── Report generation ─────────────────────────────────────────────────
reportlab==4.2.0
python-docx==1.1.0
openpyxl==3.1.5

# ── HTTP client ───────────────────────────────────────────────────────
requests==2.32.0
httpx==0.27.0

# ── Visualization ─────────────────────────────────────────────────────
matplotlib==3.9.0
seaborn==0.13.2

# ── Explainability ────────────────────────────────────────────────────
shap==0.46.0

# ── Utilities ─────────────────────────────────────────────────────────
python-dotenv==1.0.1
orjson==3.10.0

# ══════════════════════════════════════════════════════════════════════
# Level 2-3: Advanced ML & Quantum Chemistry (install separately)
# ══════════════════════════════════════════════════════════════════════

# ── Chemprop — Message Passing Neural Networks (MIT License) ──────────
# pip install chemprop
# GitHub: https://github.com/chemprop/chemprop
# chemprop>=1.6.0

# ── DeepChem — Deep learning for chemistry (MIT License) ─────────────
# pip install deepchem
# GitHub: https://github.com/deepchem/deepchem
# deepchem>=2.7.0

# ── PyTorch — Required by Chemprop and DeepChem ──────────────────────
# pip install torch
# https://pytorch.org/get-started/locally/
# torch>=2.0.0

# ── Hugging Face Transformers — ChemBERTa (Apache 2.0) ──────────────
# pip install transformers
# transformers>=4.30.0

# ── Psi4 — Quantum chemistry, DFT (LGPL3, free) ─────────────────────
# conda install -c conda-forge psi4
# https://psicode.org/
# Not pip-installable, use conda

# ── OpenMM — Molecular dynamics (MIT License) ────────────────────────
# conda install -c conda-forge openmm
# https://openmm.org/
# openmm>=8.0.0

# ── LLM APIs — Literature extraction ─────────────────────────────────
# pip install openai anthropic
# openai>=1.0.0
# anthropic>=0.20.0
